import { AddClassCardDirective } from './add-class-card.directive';

describe('AddClassCardDirective', () => {
  it('should create an instance', () => {
    const directive = new AddClassCardDirective();
    expect(directive).toBeTruthy();
  });
});
