import { MatchPwdDirective } from './match-pwd.directive';

describe('MatchPwdDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchPwdDirective();
    expect(directive).toBeTruthy();
  });
});
